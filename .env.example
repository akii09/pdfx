# ─── pdfx local development environment ─────────────────────────────────────
#
# Copy this file to .env and fill in your own paths.
#   cp .env.example .env
#
# .env is git-ignored; .env.example is committed as the canonical template.
# ─────────────────────────────────────────────────────────────────────────────

# ── scripts/sync-to-project.mjs ──────────────────────────────────────────────

# Absolute or relative path (from repo root) to the project you want to sync
# built registry components into. CLI arg takes priority over this value.
# Example (relative): ../my-pdf-app
# Example (absolute): /Users/yourname/projects/my-pdf-app
PDFX_TARGET_PROJECT=../react-test

# Absolute or relative path (from repo root) to the built registry directory.
# Only change this if you've customised the registry output location.
# Default: apps/www/public/r
PDFX_REGISTRY_DIR=apps/www/public/r
