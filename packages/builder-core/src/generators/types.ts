import type { BuilderState } from '../serializers/types';

/**
 * Generate a .types.ts interface file from builder state
 */
export function generateTypes(_state: BuilderState): string {
  // Implementation in Phase 3, Week 9
  return '';
}
